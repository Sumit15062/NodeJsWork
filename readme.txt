1)first do npm install to install all packaes of node package manager

2)then to run node project go to project directory and do nodemon index.js

3)crate some users and make one user as isAdmin as true in database

4)Then in controller file we have all the API's which are mentioned in the task
